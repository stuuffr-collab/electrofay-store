🎯 الهدف:
ربط المتجر الإلكتروني الخاص بي (مبني بـ React أو Next.js) بخدمة Supabase بحيث يتم:

حفظ بيانات المنتجات والطلبات والمستخدمين في قاعدة البيانات PostgreSQL الخاصة بـ Supabase.

تخزين صور المنتجات في وحدة التخزين Supabase Storage وربطها بالمنتجات.

تفعيل مصادقة بسيطة (إذا لزم الأمر) للمستخدمين أو لوحة الإدارة.

تنفيذ كل العمليات (إضافة، حذف، تعديل، استعلام) باستخدام مكتبة @supabase/supabase-js بشكل آمن ومنظّم.

✅ التفاصيل الدقيقة المطلوبة:
1️⃣ إعداد العميل (Client Initialization)
إنشاء ملف supabaseClient.ts وربطه بالمفاتيح من .env.local.

استخدام createClient بشكل صحيح وربط كل المكونات به.

ts
Copy
Edit
import { createClient } from "@supabase/supabase-js";

export const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
);
2️⃣ إنشاء الجداول التالية في Supabase:
الجدول	الحقول المطلوبة
products	id, name, description, price, image_url, stock, category, created_at
orders	id, customer_name, phone, total_price, status, created_at, items (JSON array)
users (اختياري)	id, email, name, created_at

ملاحظة: حقل items في الطلب يحتوي على [{product_id, quantity}]

3️⃣ مثال: استعلام المنتجات وعرضها
تنفيذ استعلام جلب كل المنتجات الفعّالة:

ts
Copy
Edit
const { data, error } = await supabase
  .from('products')
  .select('*')
  .eq('is_active', true);
4️⃣ مثال: حفظ طلب جديد عند Checkout
ts
Copy
Edit
await supabase.from('orders').insert({
  customer_name: "اسم الزبون",
  phone: "رقم الهاتف",
  total_price: 300,
  items: [{ id: "p1", qty: 2 }, { id: "p2", qty: 1 }],
});
5️⃣ تخزين صور المنتجات في Supabase Storage
رفع صورة منتج إلى bucket اسمه product-images:

ts
Copy
Edit
const { data, error } = await supabase.storage
  .from('product-images')
  .upload(`products/${file.name}`, file);
ثم استرجاع الرابط:

ts
Copy
Edit
const publicUrl = supabase.storage
  .from('product-images')
  .getPublicUrl(`products/${file.name}`).data.publicUrl;
6️⃣ حماية البيانات:
لا يتم تخزين مفاتيح API مباشرة في الكود – يتم استخدام .env.local فقط.

لا توجد عمليات CRUD إلا بعد التحقق من صلاحية المستخدم إن وُجد Auth.

7️⃣ إعداد لوحة Supabase:
أنشئ قاعدة البيانات والجداول باستخدام SQL أو واجهة Supabase مباشرة.

أنشئ bucket للتخزين (صور المنتجات).

جهّز سياسة أمان RLS (Row-Level Security) إذا لزم الأمر.

📌 النتيجة المتوقعة:
عند عرض المنتجات، يتم جلبها من Supabase.

عند الطلب، يتم حفظ بيانات الطلب في جدول orders.

الصور مرفوعة إلى Supabase Storage وروابطها تُعرض على الموقع.

الكود منظم وآمن وقابل للتوسعة.

🔒 الملاحظة الأخيرة:
يرجى الحفاظ على أمان المفاتيح واتباع أفضل ممارسات الاتصال بخدمات Supabase، وعدم تخزين بيانات حساسة دون حماية مناسبة (مثلاً سياسة Row-Level Security إن لزم).